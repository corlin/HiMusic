# HiMusic 资料库界面设计验收

## 验收输入

- 设计基准：`artifacts/design-qa/source-option-1-normalized.png`（选定视觉稿的仓库内归一化副本）
- 实现截图：`docs/screenshots/implementation-option-1.png`
- 整体并排对照：`artifacts/design-qa/comparison-full.png`
- 播放器局部对照：`artifacts/design-qa/comparison-player.png`
- 视口：1440 × 1024，设备像素比 1.0
- 基准图原始尺寸：1487 × 1058；对照前等比缩放并居中裁切为 1440 × 1024
- 状态：连接 `BE7200 MAX / USB Music`，显示六首 FLAC，第一首正在播放，使用真实 FLAC 生成动态波形

## 必检界面

| 界面 | 结果 | 证据 |
| --- | --- | --- |
| 宽屏资料库 | 通过 | 左侧导航、顶部搜索、最近播放、曲目表与底部播放器在 1440 × 1024 内完整显示 |
| 最近播放封面 | 通过 | 六张 512 × 512 位图清晰显示，裁切比例一致，无占位图和网络依赖 |
| 曲目表与选中态 | 通过 | 表头、分隔线、格式信息、播放入口和当前曲目绿色选中态清晰可辨 |
| 正在播放 | 通过 | 独立现场歌手封面、标题、来源、控制、双层动态波形、输出设备和音量完整显示 |
| 空资料库 | 通过 | 既有本地目录与 SMB 连接入口保留，未连接时仍可进入核心流程 |
| 窄窗口 | 通过 | 侧栏收拢为顶部操作区，播放器改为纵向布局，无固定宽度溢出 |

## 视觉对照

| 维度 | 结论 |
| --- | --- |
| 构图 | 232 px 左侧栏、内容区和常驻播放器的比例与基准一致；播放器最终高度约 181 px，接近基准约 176 px |
| 字体与层级 | 使用系统字体；标题、导航、辅助信息和表格信息保持四级层次，未出现截断关键操作的情况 |
| 色彩 | 深森林绿背景、浅绿色主操作、灰绿色次级文字和低对比边框与基准一致 |
| 间距与圆角 | 专辑卡片、曲目行、搜索框、选中态与播放器的间距和圆角统一，未发现碰撞或非预期空洞 |
| 图片质量 | 全部封面为 512 × 512 PNG；正在播放使用独立封面，避免与专辑陈列重复造成状态混淆 |
| 文案 | 使用真实文件名、来源、格式和文件大小；未虚构尚未解析的艺术家、专辑、位深或采样率 |

## 修正记录

1. 首轮对照发现底部播放器约 234 px，高于基准约 176 px，属于 P2。将细节波形与概览波形加入 68 px 紧凑模式后，播放器约 181 px，问题关闭。
2. 首轮实现复用了专辑陈列封面作为正在播放封面，降低了状态辨识度，属于 P2。新增独立现场歌手位图并接入播放器，问题关闭。
3. 实现保留真实文件元数据边界：当前扫描器没有艺术家、专辑、位深和采样率解析能力，因此曲目表展示文件名、路径位置、格式和大小。这是有意差异，不构成视觉缺陷。

## 剩余差异

- P3：基准中的账户头像、收藏、筛选和排序没有对应的家庭账户或索引数据模型，当前版本不展示这些无效控件。
- P3：资料库内容密度取决于实际目录；验收状态使用六首文件，基准显示十首。
- P0 / P1 / P2：无未关闭项。

final result: passed
