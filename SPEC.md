# HiMusic 产品与技术规格

- 日期：2026-09-05
- 状态：需求基线草案；尚未实现，技术选型待原型验证
- 用途：保存本次讨论结论，作为后续设计、开发和验收依据

## 1. 产品定位

面向家庭多人使用的跨平台音乐播放器。音乐来自家庭共享硬盘或设备本地文件，优先在局域网直接读取原始音频，支持 FLAC 等无损格式。

共享同一份音乐库，各设备可以独立播放。后续扩展手机遥控电视、离线下载和成员数据跨设备同步。

## 2. 已确认的使用环境

| 项目 | 当前条件 |
| --- | --- |
| 共享存储 | 中兴问天 BE7200 MAX 路由器连接 USB 共享硬盘 |
| 文件访问 | 使用路由器的 SMB 共享能力 |
| NAS / 常开服务器 | 当前没有，不作为第一版前提 |
| 电视平台 | Android TV / Google TV |
| 电视音频输出 | 内置扬声器 |
| 最终客户端范围 | Windows、macOS、iOS、Android、Android TV / Google TV |
| 音源 | SMB 共享音乐文件、设备本地文件 |
| 核心格式要求 | FLAC 等无损音频，包含高解析度文件兼容性验证 |

路由器当前固件、协商的 SMB 版本、访问权限、硬盘文件系统和实际性能尚未验证。公开实测可作为参考，不能视为用户设备的验收证据。

## 3. 第一版范围

### 3.1 音乐来源

- 手动添加 SMB 服务器地址、共享名称、音乐目录和凭据。
- 验证连接，区分认证失败、路径不存在、网络不可达等错误。
- 浏览文件夹并播放文件；后台扫描标签，形成专辑、歌手和歌曲索引。
- 支持设备本地文件来源；各平台导入和目录授权方式分别适配。
- 本地导入的文件不会自动共享给家庭其他设备。
- 默认只读访问音乐文件，不提供删除、重命名或修改源文件功能。

### 3.2 音乐库

- 提供文件夹、歌曲、专辑和歌手入口及基础搜索。
- 解析标题、艺术家、专辑艺术家、专辑、碟号、曲目号、时长和音频规格。
- 支持内嵌封面及目录封面；封面选择规则在实现时明确。
- 优先展示可用目录，再渐进补齐标签和封面。
- 索引、缩略图及个人状态保存在各设备本地。
- 后台扫描限制并发并让播放读取优先；后续扫描应尽量复用未变化文件的结果。

### 3.3 播放

- 播放、暂停、上一首、下一首、拖动进度、队列、顺序/随机/循环模式。
- SMB 音频边读边播，支持按位置读取，不要求完整下载后才播放。
- 有界缓存、读取超时、重连及明确的错误状态。
- 网络中断或设备休眠后保留队列和可恢复的位置；恢复策略避免意外自动出声。
- 手机后台播放、锁屏控制和音频中断处理。
- 无缝播放作为目标，须以实际音频与设备验证，不能仅凭插件声明验收。

### 3.4 音质与格式

- 第一版验证格式：FLAC、MP3、AAC、ALAC、WAV；具体容器和编码组合记录在测试矩阵中。
- FLAC 验证规格至少包括双声道 16-bit / 44.1 kHz、24-bit / 96 kHz、24-bit / 192 kHz。
- 读取原文件并在客户端解码；第一版没有服务端转码能力。
- 无法播放时说明原因，不默默降级或伪报格式支持。
- 播放详情区分源文件规格、传输方式和实际设备输出。无法检测输出时显示未知。
- FLAC 无损播放不等于高解析度输出，更不等于 bit-perfect。
- 第一版不承诺电视原样输出 192 kHz，不实现独占输出、外置 DAC 专项支持或 bit-perfect 保证。

### 3.5 家庭使用

- 多台设备共享源文件，播放队列独立，切歌不影响其他设备。
- 收藏、歌单、播放历史第一版保存在本机，不承诺跨设备同步。
- 电视使用客厅公共播放队列。
- SMB 访问凭据与未来家庭成员身份是两个概念，不把共享文件账号视为成员系统。

### 3.6 平台交互

- 手机：触摸操作、后台播放和系统媒体控制。
- 桌面：键鼠操作、窗口布局和平台媒体控制适配。
- 电视：大字号、清晰焦点、方向键导航、返回路径、简化搜索与播放页。
- 电视不得依赖触摸才能完成核心流程；不能直接放大手机页面作为验收版本。

## 4. 建议技术架构

以下为拟采用方案，不代表已完成跨平台可行性验证。

```text
路由器 USB 硬盘 / SMB                       设备本地文件
          │                                     │
          └──────── 音乐来源适配层 ───────────────┘
                           │
              目录扫描 / 标签解析 / 本地索引
                           │
                  播放队列与播放控制
                           │
                  平台音频引擎适配层
                           │
              手机 / 桌面 / 电视音频输出
```

- UI 与业务层拟采用 Flutter，共享核心逻辑，分别适配三类界面。
- SQLite 保存本机索引和状态，文件目录保存音频缓存与缩略图。
- 不通过 SMB 让多台设备直接共用一个 SQLite 数据库。
- 音乐来源、索引、播放会话和平台音频实现分层，便于未来接入 NAS 音乐服务。
- 歌曲身份不以某台设备的绝对路径作为唯一跨设备标识；跨设备匹配策略在同步阶段定义。
- 播放引擎候选为 just_audio，手机后台能力候选为 audio_service；桌面依赖和系统集成单独验证。
- SMB 库尚未确定，需核验各目标平台支持、许可证、随机读取、取消、重连及大文件能力。
- 不假设音频引擎直接接受 smb:// 地址；原型比较可定位数据源与设备内 HTTP 桥接两种接入方式。
- 若采用本地 HTTP 桥接，仅监听回环地址，限制资源访问范围，实现范围读取与生命周期清理。
- SMB 凭据使用系统安全存储，日志不记录密码。
- 路由器只承担文件共享；不要求安装 Navidrome、Docker 或转码服务。

## 5. 版本与验收顺序

### V0：实机可行性原型

优先真实 Android TV，再验证 Android 手机与一个桌面端；其余目标平台保留工程入口并尽早完成构建验证。

验收闭环：连接共享目录 → 浏览音乐 → 播放 FLAC → 拖动 → 连续切歌 → 中断恢复。

| 验收项 | 通过条件 |
| --- | --- |
| SMB 连接 | 能在用户路由器上认证、列目录和读取文件，记录实际协议与固件 |
| 格式 | 代表性文件播放正确，记录格式、位深、采样率、设备和结果 |
| 流式读取 | 大文件无需完整下载即可播放，缓存占用有界 |
| 拖动 | 可前后定位并恢复播放，不错误跳曲或丢失队列 |
| 多人并发 | 至少两台设备同时播放不同曲目，控制与队列互不影响 |
| 异常恢复 | 网络中断、硬盘唤醒及电视休眠后状态明确，能恢复或主动重试 |
| 遥控器 | 仅靠方向键、确认和返回即可完成核心流程 |
| 连续播放 | 连续专辑播放无异常停止；无缝效果单独记录 |

延迟、扫描耗时和缓存预算在原型测量后确定，不预先声明未经测量的性能指标。

### V1：日常家庭播放器

- 完成库浏览、标签索引、搜索、收藏、歌单和队列。
- 完成手机、桌面、电视布局及各平台必要音频集成。
- 建立所有目标平台的真实验证矩阵，未验证平台不得标记已支持。

### V2：设备协作与离线

- 手机与电视局域网配对、手机遥控、公共队列权限。
- 电视直接读取 SMB；手机退出不应中断电视播放。
- 离线下载、空间管理、下载恢复。
- 家庭成员数据同步另行设计，不以共享数据库文件实现。

### V3：按实际需要扩展

- 跨设备播放接力、家庭协作歌单。
- 外网访问架构。
- 可选 Navidrome / OpenSubsonic 等服务器来源。
- 多房间同步需单独规格，不能等同于同时发送播放命令。

## 6. 暂不纳入基础承诺

- 网易云、QQ 音乐、Apple Music 等在线平台接入。
- 云端账号、付费订阅、自动上传本地文件。
- Apple TV、三星 Tizen、LG webOS。
- APE、DSD、SACD ISO、整轨 FLAC + CUE；是否提前支持取决于实际曲库。
- 服务端转码、路由器安装应用、bit-perfect 和多房间同步。

## 7. 待验证信息

1. 路由器固件、实际 SMB 版本、认证与只读权限能力。
2. 硬盘文件系统、容量、供电及休眠恢复表现。
3. 电视型号、系统版本、可用存储和解码表现。
4. 曲库规模、标签质量、中文与特殊字符路径、CUE/APE 等文件占比。
5. 各平台 SMB 库与音频引擎组合的实际可用性。

上述信息在开发原型时收集，不阻碍保存本规格。

## 8. 参考资料与证据边界

- [Flutter 支持平台](https://docs.flutter.dev/reference/supported-platforms)：平台框架范围，不代表音频及 SMB 插件均可直接使用。
- [Android 媒体格式](https://developer.android.com/media/platform/supported-formats)
- [Media3 格式支持](https://developer.android.com/media/media3/exoplayer/supported-formats)
- [Android TV 导航](https://developer.android.com/training/tv/get-started/navigation)
- [just_audio 文档](https://pub.dev/documentation/just_audio/latest/index.html)
- [audio_service](https://pub.dev/packages/audio_service)
- [BE7200 MAX 第三方实测](https://post.smzdm.com/p/ago77v4d/)：参考其 USB/Samba 使用记录；非官方规格，不能据此确认用户固件版本、权限或性能。

文档保存时未进行用户路由器、共享硬盘或电视的实机测试。
